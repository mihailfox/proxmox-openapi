{
  "id": "fzf",
  "name": "fzf fuzzy finder",
  "version": "1.0.0",
  "description": "Installs fzf for interactive fuzzy search in shell",
  "options": {
    "method": {
      "type": "string",
      "enum": ["apt", "gh-release"],
      "default": "apt",
      "description": "Install via apt or from latest GitHub release asset"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils:2"
  ]
}
